# Зависимости для тестирования Rate Limit Optimizer

# Основные тестовые фреймворки
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-benchmark>=4.0.0
pytest-xdist>=3.3.0  # Для параллельного запуска тестов

# HTTP мокинг и тестирование
aioresponses>=0.7.4
responses>=0.23.0
httpx>=0.24.0
requests-mock>=1.11.0

# Async и сетевые библиотеки
aiohttp>=3.8.0
asyncio-mqtt>=0.13.0  # Если используется MQTT
websockets>=11.0.0  # Если используется WebSocket

# Валидация данных и модели
pydantic>=2.0.0
jsonschema>=4.17.0

# AI и ML библиотеки для тестирования
openai>=1.0.0  # Для мокинга OpenAI API
anthropic>=0.7.0  # Для мокинга Anthropic API

# Мониторинг и метрики
psutil>=5.9.0  # Для мониторинга ресурсов
memory-profiler>=0.60.0  # Для профилирования памяти
py-spy>=0.3.14  # Для профилирования производительности

# Утилиты для работы с данными
pandas>=2.0.0  # Для анализа результатов тестов
numpy>=1.24.0  # Для статистических вычислений
matplotlib>=3.7.0  # Для графиков производительности

# Работа с файлами и сериализация
PyYAML>=6.0
openpyxl>=3.1.0  # Для экспорта в Excel
xlsxwriter>=3.1.0  # Альтернативный Excel writer

# Криптография и безопасность
cryptography>=41.0.0  # Для тестирования шифрования
bcrypt>=4.0.0  # Для хеширования паролей в тестах

# Базы данных для тестирования
redis>=4.5.0  # Для тестирования кэширования
fakeredis>=2.17.0  # Мок Redis для тестов
sqlite3  # Встроенная, для тестовых БД

# Временные и дата/время утилиты
freezegun>=1.2.0  # Для мокинга времени
python-dateutil>=2.8.0

# Логирование и отладка
structlog>=23.1.0  # Структурированное логирование
colorlog>=6.7.0  # Цветное логирование для тестов

# Конфигурация и переменные окружения
python-dotenv>=1.0.0
configparser>=5.3.0

# Сетевые утилиты
netifaces>=0.11.0  # Для получения сетевых интерфейсов
dnspython>=2.4.0  # Для DNS запросов в тестах

# Параллельность и многопоточность
concurrent-futures>=3.1.1  # Для тестирования параллельности
threading-utils>=0.3  # Утилиты для работы с потоками

# Мокинг и фейковые данные
factory-boy>=3.3.0  # Для создания тестовых объектов
faker>=19.0.0  # Для генерации фейковых данных
mimesis>=11.1.0  # Альтернативный генератор данных

# Качество кода и линтинг (для CI/CD)
flake8>=6.0.0
black>=23.7.0
isort>=5.12.0
mypy>=1.5.0
bandit>=1.7.5  # Проверка безопасности
safety>=2.3.0  # Проверка уязвимостей в зависимостях

# Документация и отчеты
sphinx>=7.1.0  # Для генерации документации
sphinx-rtd-theme>=1.3.0
coverage>=7.3.0  # Покрытие кода
pytest-html>=3.2.0  # HTML отчеты тестов
pytest-json-report>=1.5.0  # JSON отчеты

# Интеграция с внешними сервисами
docker>=6.1.0  # Для Docker интеграционных тестов
kubernetes>=27.2.0  # Для K8s тестов
boto3>=1.28.0  # Для AWS тестов
google-cloud-storage>=2.10.0  # Для GCP тестов

# Специфичные для проекта зависимости
anthropic>=0.7.0  # Для AI рекомендаций
openrouter>=0.1.0  # Если есть специальный клиент

# Дополнительные утилиты
click>=8.1.0  # Для CLI тестирования
rich>=13.5.0  # Для красивого вывода в тестах
tabulate>=0.9.0  # Для форматирования таблиц
tqdm>=4.66.0  # Прогресс бары для длительных тестов

# Специализированные тестовые библиотеки
hypothesis>=6.82.0  # Property-based тестирование
mutmut>=2.4.0  # Mutation тестирование
locust>=2.16.0  # Нагрузочное тестирование
